<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
	<div class="container-fluid">
		<a class="navbar-brand" href="#">Navbar</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarColor01">
			<ul class="navbar-nav me-auto">
				<li class="nav-item">
					<a class="nav-link " href="{{path('home')}}">Home
						<span class="visually-hidden">(current)</span>
					</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{path('category_index')}}">
					Catalogue
					
					</a>
				</li>
			</ul>
				<div class='d-flex me-4'>
				<ul class="navbar-nav me-auto">
					<li class="nav-item dropdown">
						<a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Panier</a>
						<div class="dropdown-menu">
	<div class="list-group">
		{% for item in navCart.items %}<a class="list-group-item list-gorup-item-action flex-column align-items-start" >
		<div class="d-flex w-100 justify-content-between"><h6>{{item.product.name}}</h6>
		<p>{{item.product.price | format_currency('EUR')}}</p>
		<small>Quantity : {{item.quantity}}</small></div></a> {% endfor %}
	</div>
								<a class="nav-link " href="{{path('cart')}}">Mon panier</a>
							

						</div>
					</li>
				</div>
			</ul>
			<div class='d-flex me-4'>
				<ul class="navbar-nav me-auto">
					<li class="nav-item dropdown">
						<a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">{% if app.user %}{{app.user.name}}{% else %} Utilisateur {% endif %}</a>
						<div class="dropdown-menu">

							{% if app.user %}
								<a href="{{path('user_profil', {id : app.user.id })}}" class="nav-link">Info</a>
								<a href="{{path('user_orders', {id : app.user.id })}}" class="nav-link">Mes Commandes</a>
								<div class="dropdown-divider"></div>
								<a class="nav-link" href="{{path('security_logout', {id : app.user.id })}}">Deconnexion</a>


							{% else %}
								<a class="nav-link " href="{{path('security_registration')}}">S'inscrire</a>
								<a class="nav-link" href="{{path('security_login')}}">Connexion</a>
							{% endif %}

						</div>
					</li>
				</div>
			</ul>
		</div>
			
		</div>
	</div>
	
	
</nav>

<nav class="navbar navbar-expand-lg navbar-dark bg-info justify-content-center">
<ul class="nav  navbar-nav  ">
{% for category in liste_categories %}
{% if category.childs == null  %}
<li class="nav-item active"><a href="{{ path('category_subcategories',{'category': category.id })}}" class="nav-link">{{category.name}}</a></li>
{% endif %}

{% endfor %}</ul>
</nav>

