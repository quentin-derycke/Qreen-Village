<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
	<div class="container-fluid">
		<a class="navbar-brand" href="#">Navbar</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarColor01">
			<ul class="navbar-nav me-auto">
				<li class="nav-item">
					<a class="nav-link " href="{{path('home')}}">Home
						<span class="visually-hidden">(current)</span>
					</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{path('category_index')}}">Categories</a>
				</li>
			</ul>
				<div class='d-flex me-4'>
				<ul class="navbar-nav me-auto">
					<li class="nav-item dropdown">
						<a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Panier</a>
						<div class="dropdown-menu">

								<a class="nav-link " href="{{path('cart')}}">Mon panier</a>
							

						</div>
					</li>
				</div>
			</ul>
		</div>
			<div class='d-flex me-4'>
				<ul class="navbar-nav me-auto">
					<li class="nav-item dropdown">
						<a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Profile</a>
						<div class="dropdown-menu">

							{% if app.user %}
								<a href="{{path('user_profil', {id : app.user.id })}}" class="nav-link">Info</a>
								<div class="dropdown-divider"></div>
								<a class="nav-link" href="{{path('security_logout', {id : app.user.id })}}">Deconnexion</a>


							{% else %}
								<a class="nav-link " href="{{path('security_registration')}}">S'inscrire</a>
								<a class="nav-link" href="{{path('security_login')}}">Connexion</a>
							{% endif %}

						</div>
					</li>
				</div>
			</ul>
		</div>
	</div>
</nav>
